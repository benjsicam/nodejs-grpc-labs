syntax = "proto3";

package streaming;

message RequestArgs {
  int32 numOfParagraphs = 1;
}

message Result {
  string paragraph = 1;
}

service LoremIpsumService {
  rpc generate (RequestArgs) returns (stream Result) {}
}